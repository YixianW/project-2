/**
 * API Configuration
 * 
 * 设置 window.API_BASE_URL 来指定后端 API 的基础 URL
 * 
 * 使用场景：
 * 1. 本地开发：不需要设置，默认使用相对路径 (http://localhost:5000/api/analyze)
 * 2. GitHub Pages + Render 分离部署：设置为你的 Render 应用 URL
 *    例如：window.API_BASE_URL = 'https://your-app.onrender.com'
 * 
 * 取消注释下面的行并修改为你的 Render 应用 URL
 */

// window.API_BASE_URL = 'https://your-app.onrender.com';
